# Canonical site origin used for canonical URLs, hreflang, sitemap, JSON-LD and llms.txt.
# Set this in Vercel → Project → Settings → Environment Variables (Production).
# No secrets are required to build or run this site.
NEXT_PUBLIC_SITE_URL=https://voidvision.ai
